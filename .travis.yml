language: python

python:
  - "3.4"
virtualenv:
  system_site_packages: true
  
addons:
  apt:
    packages:
      - python3-pyqt5
      - python3-pip

install:
 - pip install cx_freeze==4.3.3
 - pip install libyaz0
 - pip install sarclib
 
script:
 - build.py
